# Orca 0.2.4

Release focus: CI signal quality and release metadata consistency.

## Highlights

- Updated CI workflow concurrency policy to avoid canceling in-progress runs on new pushes.
- Pinned README CI badge to `main` branch to prevent misleading status from unrelated refs.
- Bumped project default version to `0.2.4`.

No renderer/parser behavior changes in this release.

## Maven Coordinates

- `ru.wertik:orca-core:0.2.4`
- `ru.wertik:orca-compose:0.2.4`

## Verification

```bash
./gradlew --no-daemon --build-cache :orca-core:test :orca-compose-android:testDebugUnitTest :sample-app:assembleDebug
```
